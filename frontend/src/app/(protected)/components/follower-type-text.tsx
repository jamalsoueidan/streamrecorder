import { FollowerTypeEnum } from "@/lib/types/strapi-autogenerated";
import { Image, ThemeIconProps } from "@mantine/core";

type FollowerTypeTextProps = {
  type: FollowerTypeEnum | undefined;
} & ThemeIconProps;

export const FollowerTypeText = ({ type }: FollowerTypeTextProps) => {
  return (
    <>
      {type === FollowerTypeEnum.Tiktok && (
        <Image src="tiktok.svg" alt="Tiktok" w={50} fit="contain" />
      )}
      {type === FollowerTypeEnum.Twitch && (
        <Image src="twitch.svg" alt="Twitch" w={50} fit="contain" />
      )}
      {type === FollowerTypeEnum.Kick && (
        <Image src="kick.svg" alt="Kick" w={50} fit="contain" />
      )}
      {type === FollowerTypeEnum.Youtube && (
        <Image src="youtube.svg" alt="Youtube" w={50} fit="contain" />
      )}
      {type === FollowerTypeEnum.Afreecatv && (
        <Image src="afreecatv.svg" alt="Afreecatv" w={50} fit="contain" />
      )}
      {type === FollowerTypeEnum.Pandalive && (
        <Image src="pandalive.png" alt="Pandalive" w={50} fit="contain" />
      )}
    </>
  );
};
